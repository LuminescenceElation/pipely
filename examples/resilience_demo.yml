name: resilience_demo

jobs:
  retry_demo:
    steps:
      - name: flaky step with retries
        run: bash -c "exit 1"
        retries: 2
        retry_delay_seconds: 1

  timeout_demo:
    steps:
      - name: step that hangs
        run: sleep 10
        timeout_seconds: 2
