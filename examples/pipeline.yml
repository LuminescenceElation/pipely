name: dependency_demo

jobs:
  build:
    steps:
      - run: echo "building..."

  test:
    needs: build
    steps:
      - run: echo "running tests..."

  deploy:
    needs: [build, test]
    steps:
      - run: echo "deploying..."
